import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Search } from "lucide-react";
import { useState } from "react";
import { Input } from "@/components/ui/input"; import { EmptyState, ErrorState, LoadingState, PageHeader } from "@/components/page-ui"; import { useSimbiData } from "@/lib/simbi-data";
export const Route=createFileRoute("/_authenticated/students")({head:()=>({meta:[{title:"Alunos — Simbi"},{name:"description",content:"Acesse a memória individual dos seus alunos."},{property:"og:title",content:"Alunos — Simbi"},{property:"og:description",content:"Acompanhamento individual na sua rotina docente."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:StudentsPage});
function StudentsPage(){const{students,classes,links,loading,error,refresh}=useSimbiData();const[q,setQ]=useState("");if(loading)return <LoadingState/>;if(error)return <ErrorState message={error} retry={()=>void refresh()}/>;const filtered=students.filter(s=>s.name.toLowerCase().includes(q.toLowerCase()));return <><PageHeader eyebrow="Acompanhamento" title="Alunos" description="Encontre rapidamente os registros em que cada aluno apareceu."/><div className="relative mb-6"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={18}/><Input className="h-11 pl-10" value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar aluno"/></div>{filtered.length?<div className="divide-y divide-border border-y border-border">{filtered.map(student=><Link key={student.id} to="/students/$studentId" params={{studentId:student.id}} className="group grid grid-cols-[minmax(0,1fr)_auto] items-center gap-4 py-4"><div className="flex min-w-0 items-center gap-4"><span className="grid size-10 shrink-0 place-items-center rounded-full bg-secondary font-display font-bold text-primary">{student.name.charAt(0).toUpperCase()}</span><div className="min-w-0"><h2 className="truncate font-semibold">{student.name}</h2><p className="text-sm text-muted-foreground">{classes.find(c=>c.id===student.class_id)?.name} · {links.filter(l=>l.student_id===student.id).length} registros</p></div></div><ArrowRight className="text-muted-foreground transition-transform group-hover:translate-x-1" size={18}/></Link>)}</div>:<EmptyState title="Nenhum aluno encontrado" text={q?"Tente buscar por outro nome.":"Os alunos adicionados às turmas aparecerão aqui."}/>}</>}
